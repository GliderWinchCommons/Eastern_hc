create table "APP".AIRFIELD
(
	AIRFIELD_ID INTEGER not null primary key,
	NAME VARCHAR(30),
	DESIGNATOR VARCHAR(30),
	ELEVATION DOUBLE,
	MAGNETIC_VARIATION DOUBLE,
	LATITUDE DOUBLE,
	LONGITUDE DOUBLE,
	UTC_OFFSET INTEGER,
	OPTIONAL_INFO LONG VARCHAR
);
create table "APP".AIRFIELDUNITS
(
	UNIT_SET INTEGER not null primary key,
	DISTANCE_UNIT INTEGER
);
create table "APP".CAPABILITY
(
	CAPABILITY_ID INTEGER not null primary key,
	CAPABILITY_STRING VARCHAR(15)
);
create table "APP".DASHBOARDUNITS
(
	UNIT_SET INTEGER not null primary key,
	DISTANCE_UNIT INTEGER,
	TENSION_UNIT INTEGER,
	VELOCITY_UNIT INTEGER
);
create table "APP".DISTANCEUNITS
(
	INDEX INTEGER not null primary key,
	ABBREVIATION VARCHAR(5)
);
create table "APP".DRUM
(
	DRUM_ID INTEGER not null primary key,
	WINCH_ID INTEGER,
	DRUM_NAME VARCHAR(30),
	DRUM_NUMBER INTEGER,
	CORE_DIAMETER DOUBLE,
	KFACTOR DOUBLE,
	SPRING_CONST DOUBLE,
	CABLE_LENGTH DOUBLE,
	CABLE_DENSITY DOUBLE,
	DRUM_SYSTEM_EMASS DOUBLE,
	NUMBER_OF_LAUNCHES INTEGER,
	MAXIMUM_WORKING_TENSION DOUBLE,
	OPTIONAL_INFO LONG VARCHAR
);
create table "APP".ENVIRONMENTALUNITS
(
	UNIT_SET INTEGER not null primary key,
	TEMP_UNIT INTEGER,
	PRESSURE_UNIT INTEGER
);
create table "APP".GLIDER
(
	GLIDER_ID INTEGER not null primary key,
	REG_NUMBER VARCHAR(30),
	COMMON_NAME VARCHAR(30),
	OWNER VARCHAR(30),
	TYPE VARCHAR(30),
	MAX_GROSS_WEIGHT DOUBLE,
	EMPTY_WEIGHT DOUBLE,
	INDICATED_STALL_SPEED DOUBLE,
	MAX_WINCHING_SPEED DOUBLE,
	MAX_WEAK_LINK_STRENGTH DOUBLE,
	MAX_TENSION DOUBLE,
	CABLE_RELEASE_ANGLE DOUBLE,
	CARRY_BALLAST BOOLEAN,
	MULTIPLE_SEATS BOOLEAN,
	OPTIONAL_INFO LONG VARCHAR
);
create table "APP".GLIDERPOSITION
(
	GLIDER_POSITION_ID INTEGER not null primary key,
	PARENT_ID INTEGER,
	POSITION_NAME VARCHAR(30),
	ELEVATION DOUBLE,
	LATITUDE DOUBLE,
	LONGITUDE DOUBLE,
	OPTIONAL_INFO LONG VARCHAR
);
create table "APP".GLIDERUNITS
(
	UNIT_SET INTEGER not null primary key,
	WEIGHT_UNIT INTEGER,
	VELOCITY_UNIT INTEGER,
	TENSION_UNIT INTEGER
);
create table "APP".MESSAGES
(
	TIMESTAMP BIGINT,
	MESSAGE VARCHAR(40)
);
create table "APP".OPERATOR
(
	OPERATOR_ID INTEGER not null primary key,
	FIRST_NAME VARCHAR(30),
	MIDDLE_NAME VARCHAR(30),
	LAST_NAME VARCHAR(30),
	ADMIN BOOLEAN,
	SALT VARCHAR(30),
	HASH LONG VARCHAR,
	OPTIONAL_INFO LONG VARCHAR,
	UNITSETTINGS LONG VARCHAR
);
create table "APP".PARACHUTE
(
	PARACHUTE_ID INTEGER not null primary key,
	NAME VARCHAR(30),
	LIFT DOUBLE,
	DRAG DOUBLE,
	WEIGHT DOUBLE,
	OPTIONAL_INFO LONG VARCHAR
);
create table "APP".PILOT
(
	PILOT_ID INTEGER not null primary key,
	FIRST_NAME VARCHAR(30),
	LAST_NAME VARCHAR(30),
	MIDDLE_NAME VARCHAR(30),
	FLIGHT_WEIGHT DOUBLE,
	CAPABILITY INTEGER,
	PREFERENCE DOUBLE,
	EMERGENCY_CONTACT_NAME VARCHAR(30),
	EMERGENCY_CONTACT_PHONE VARCHAR(20),
	OPTIONAL_INFO LONG VARCHAR
);
create table "APP".PILOTUNITS
(
	UNIT_SET INTEGER not null primary key,
	WEIGHT_UNIT INTEGER
);
create table "APP".POSITIONUNITS
(
	UNIT_SET INTEGER not null primary key,
	DISTANCE_UNIT INTEGER
);
create table "APP".PRESSUREUNITS
(
	INDEX INTEGER not null primary key,
	ABBREVIATION VARCHAR(5)
);
create table "APP".PREVIOUSAIRFIELDINFO
(
	TABLE_ID INTEGER not null primary key,
	AIRFIELD_NAME VARCHAR(30),
	AIRFIELD_DESIGNATOR VARCHAR(30),
	AIRFIELD_ELEVATION DOUBLE,
	AIRFIELD_MAGNETIC_VARIATION DOUBLE,
	AIRFIELD_LATITUDE DOUBLE,
	AIRFIELD_LONGITUDE DOUBLE,
	AIRFIELD_UTC_OFFSET INTEGER,
	RUNWAY_NAME VARCHAR(10),
	RUNWAY_MAGNETIC_HEADING DOUBLE,
	GLIDER_POSITION_NAME VARCHAR(30),
	GLIDER_POSITION_ELEVATION DOUBLE,
	GLIDER_POSITION_LATITUDE DOUBLE,
	GLIDER_POSITION_LONGITUDE DOUBLE,
	WINCH_POSITION_NAME VARCHAR(30),
	WINCH_POSITION_ELEVATION DOUBLE,
	WINCH_POSITION_LATITUDE DOUBLE,
	WINCH_POSITION_LONGITUDE DOUBLE,
	WINCH_NAME VARCHAR(30),
	WINCH_OWNER VARCHAR(30)
);
create table "APP".PREVIOUSLAUNCHES
(
	START_TIMESTAMP TIMESTAMP not null primary key,
	END_TIMESTAMP TIMESTAMP,
	FIRST_NAME VARCHAR(30),
	LAST_NAME VARCHAR(30),
	MIDDLE_NAME VARCHAR(30),
	FLIGHT_WEIGHT DOUBLE,
	CAPABILITY INTEGER,
	PREFERENCE DOUBLE,
	EMERGENCY_CONTACT_NAME VARCHAR(30),
	EMERGENCY_CONTACT_PHONE VARCHAR(20),
	PILOT_OPTIONAL_INFO LONG VARCHAR,
	REG_NUMBER VARCHAR(30),
	COMMON_NAME VARCHAR(30),
	GLIDER_OWNER VARCHAR(30),
	TYPE VARCHAR(30),
	MAX_GROSS_WEIGHT DOUBLE,
	EMPTY_WEIGHT DOUBLE,
	INDICATED_STALL_SPEED DOUBLE,
	MAX_WINCHING_SPEED DOUBLE,
	MAX_WEAK_LINK_STRENGTH DOUBLE,
	MAX_TENSION DOUBLE,
	CABLE_RELEASE_ANGLE DOUBLE,
	CARRY_BALLAST BOOLEAN,
	MULTIPLE_SEATS BOOLEAN,
	GLIDER_OPTIONAL_INFO LONG VARCHAR,
	WIND_DIRECTION_WINCH DOUBLE,
	WIND_AVERAGE_SPEED_WINCH DOUBLE,
	WIND_GUST_SPEED_WINCH DOUBLE,
	WIND_DIRECTION_GLIDER DOUBLE,
	WIND_AVERAGE_SPEED_GLIDER DOUBLE,
	WIND_GUST_SPEED_GLIDER DOUBLE,
	DENSITY_ALTITUDE DOUBLE,
	TEMPERATURE DOUBLE,
	ALTIMETER_SETTING DOUBLE,
	BALLAST DOUBLE,
	BAGGAGE DOUBLE,
	PASSENGER_WEIGHT DOUBLE,
	DRUM_NAME VARCHAR(30),
	DRUM_NUMBER INTEGER,
	CORE_DIAMETER DOUBLE,
	KFACTOR DOUBLE,
	CABLE_LENGTH DOUBLE,
	CABLE_DENSITY DOUBLE,
	DRUM_SYSTEM_EMASS DOUBLE,
	NUMBER_OF_LAUNCHES INTEGER,
	MAXIMUM_WORKING_TENSION DOUBLE,
	PARACHUTE_NAME VARCHAR(30),
	PARACHUTE_LIFT DOUBLE,
	PARACHUTE_DRAG DOUBLE,
	PARACHUTE_WEIGHT DOUBLE,
	AIRFIELD_INFO INTEGER
);
create table "APP".RUNWAY
(
	RUNWAY_ID INTEGER not null primary key,
	PARENT_ID INTEGER,
	RUNWAY_NAME VARCHAR(10),
	MAGNETIC_HEADING DOUBLE,
	OPTIONAL_INFO LONG VARCHAR
);
create table "APP".TEMPERATUREUNITS
(
	INDEX INTEGER not null primary key,
	ABBREVIATION VARCHAR(5)
);
create table "APP".TENSIONUNITS
(
	INDEX INTEGER not null primary key,
	ABBREVIATION VARCHAR(5)
);
create table "APP".VELOCITYUNITS
(
	INDEX INTEGER not null primary key,
	ABBREVIATION VARCHAR(5)
);
create table "APP".VERSION
(
	DB_VERSION VARCHAR(10) not null primary key,
	HC_VERSION VARCHAR(10)
);
create table "APP".WEIGHTUNITS
(
	INDEX INTEGER not null primary key,
	ABBREVIATION VARCHAR(5)
);
create table "APP".WINCH
(
	WINCH_ID INTEGER not null primary key,
	NAME VARCHAR(30),
	OWNER VARCHAR(30),
	WC_VERSION VARCHAR(10),
	W1 DOUBLE,
	W2 DOUBLE,
	W3 DOUBLE,
	W4 DOUBLE,
	W5 DOUBLE,
	W6 DOUBLE,
	W7 DOUBLE,
	W8 DOUBLE,
	W9 DOUBLE,
	W10 DOUBLE,
	W11 DOUBLE,
	W12 DOUBLE,
	W13 DOUBLE,
	W14 DOUBLE,
	W15 DOUBLE,
	W16 DOUBLE,
	METEOROLOGICAL_CHECK_TIME INTEGER,
	METEOROLOGICAL_VERIFY_TIME INTEGER,
	RUN_ORIENTATION_TOLERANCE DOUBLE,
	OPTIONAL_INFO LONG VARCHAR
);
create table "APP".WINCHPOSITION
(
	WINCH_POSITION_ID INTEGER not null primary key,
	PARENT_ID INTEGER,
	POSITION_NAME VARCHAR(30),
	ELEVATION DOUBLE,
	LATITUDE DOUBLE,
	LONGITUDE DOUBLE,
	OPTIONAL_INFO LONG VARCHAR
);






